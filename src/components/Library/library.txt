const bookSchema = new Schema({
    name: {type: String,},
    author: {type: String,},
    year: {type: Number,},
    pages: {type: Number,}
    status: {
        type: String,
        enum: ["done", "read", "plan"],
        default: "plan",
    },
});